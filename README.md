# Zao Dash

Zao Dash is an agency operating system. It tracks clients, projects, retainers, invoices, and AI agents. This public tree is a sanitized snapshot of the private product. Household finance, CPA/tax office, and personal-bank tools are not included.

To show a different product name in the UI and docs, run `php scripts/rebrand.php`. The default name is `Agency Dash`. Pass another name with `php scripts/rebrand.php "Northstar OS"`. Details are in [docs/rebrand.md](docs/rebrand.md).

## Stack

| Layer | Version |
| --- | --- |
| PHP | 8.4 |
| Laravel | 12 |
| Inertia.js | 2 |
| Vue | 3 (Composition API) |
| Tailwind CSS | 4 |
| Laravel Horizon | 5 |
| Laravel Reverb | 1 |
| Laravel Sanctum | 4 |
| Laravel MCP | 0.x |
| Pest | 3 |

## First-run checklist

1. Install PHP 8.4, Composer, Node 20+, and SQLite (or MySQL).
2. Copy `.env.example` to `.env`.
3. Run `composer setup`. That installs PHP deps, generates `APP_KEY`, migrates, installs npm packages, and builds frontend assets.
4. Run `composer dev` for the HTTP server, queue listener, logs, and Vite.
5. Open the app URL from `.env` (`APP_URL`, default `http://localhost:8000`).
6. Sign in with the seeder owner if you ran `php artisan db:seed` (`owner@example.com` / `password`). Change that password before any shared deploy.
7. Connect only the integrations you need. Empty env values stay disabled.
8. Optional. Rebrand display copy with `php scripts/rebrand.php` before you commit a fork.
9. Optional. Mint an MCP token and connect Cursor or Claude. See [Connect a local assistant](docs/getting-started.md).

Laravel Sail is available as a Composer dev dependency if you prefer Docker. This tree does not ship a root `docker-compose.yml`. Publish Sail's compose file with `php artisan sail:install` when you want that path.

## Environment variables

Copy `.env.example` and fill values you use. Do not commit `.env`.

The complete catalog of names, status, and purpose is generated from `.env.example`, `${NAME}` placeholders in `.mcp.json`, and `env()` calls:

```bash
php scripts/catalog-env.php
```

That writes [docs/environment.md](docs/environment.md). The catalog lists names only. It never includes secret values.

Set these on first run. Leave the rest empty until you use the feature.

| Variable | Purpose |
| --- | --- |
| `APP_KEY` | Laravel encryption key. Generated by `php artisan key:generate`. |
| `APP_URL` | Public URL of the app. |
| `DB_*` | Database connection. SQLite by default. |
| `QUEUE_CONNECTION` | Queue driver. `database` locally, Redis in production. |
| `BROADCAST_CONNECTION` | Reverb in `.env.example`. |
| `MAIL_*` | Outbound mail. Defaults are placeholders. |
| `AWS_*` | S3 disk for durable uploads (needed on Laravel Cloud). |
| `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `GEMINI_API_KEY`, `GROK_API_KEY` | Agent models. |
| `SLACK_*` | Slack app OAuth and bot token. |
| `GOOGLE_*` | Gmail / Calendar / Drive OAuth. |
| `GITHUB_*` | GitHub App credentials. |
| `QUICKBOOKS_*` | QuickBooks Online. Use `sandbox` until you are ready. |
| `PAYPAL_*` | Invoicing. Keep `PAYPAL_MODE=sandbox` until go-live. |
| `COMPANY_*` | Name and address printed on invoices. |
| `BANK_*` | ACH instructions printed on invoices. Leave empty to hide. |
| `SPINUPWP_*` | WordPress hosting for the website builder. |
| `AGENT_INTERNAL_TOKEN` | Shared secret for agent callbacks. |

Integration keys that you leave blank disable that integration. The example file also lists Plaid, Teller, and tax-bridge keys so existing docs still parse. Those household modules are stubbed out of HTTP and MCP in this snapshot.

## MCP

HTTP MCP servers register in `routes/ai.php`. Step-by-step client setup is in [Connect a local assistant](docs/getting-started.md).

| Path | Server | Role |
| --- | --- | --- |
| `/mcp/zao-dash` | `ZaoDashServer` | Clients, projects, tasks, invoices, agents |
| `/mcp/zao-finance` | `ZaoFinanceServer` | Client profitability and invoice DSO only |
| `/mcp/zao-web` | `ZaoWebServer` | Website builder, WordPress, SEO |
| `/mcp/zao-comms` | `ZaoCommsServer` | Email, Slack, Drive, RFPs |
| `/mcp/zao-client` | `ZaoClientServer` | Per-client site token |

Internal servers use Sanctum plus `EnsureInternalUser`. The client server uses a client-scoped token.

## Laravel Cloud

This app is built to deploy on Laravel Cloud. Pushing the branch your Cloud environment tracks runs build, migrations, and queue restart. Set the same env vars there that you set locally. Use the `s3` disk in production. Horizon and Reverb need their Cloud add-ons enabled if you use those features.

## Tests

```bash
php artisan test
```

## License

MIT. See `LICENSE`. Composer and npm dependencies keep their own licenses. TCPDF is LGPL. FPDF uses its own permissive grant. Those vendor licenses do not change the MIT grant on this repository's source.

## What is not in this tree

Household Life ledger UI, CPA/tax office, personal-bank MCP tools, and owner PII from the private product are stripped or stubbed. Models and migrations for those tables may still exist so `php artisan migrate` stays coherent. They have no HTTP routes in this snapshot. Details are in `docs/OSS.md`.
